
#include"Cover.h"
int main()
{
	KSet *A1, *A2, *A3, *A4, *A5, *A6, *A7;
	Set *a, *b, *c, *d, *e, *f,*g;
	
	set_insert(a,)
	A1->key = A1;
	set_insert(&A1->set, a);
	Set *subsets;
	set_insert(subsets, A1);
	set_insert(subsets, A2);
	set_insert(subsets, A3);
	set_insert(subsets, A4);
	set_insert(subsets, A5);
	set_insert(subsets, A6);
	set_insert(subsets, A7);

	return 0;
}